export const convertEstimateToInvoice = async (estimateId: string): Promise<boolean> => {
  // Implementation placeholder
  return true;
};

export const deleteEstimate = async (estimateId: string): Promise<boolean> => {
  // Implementation placeholder
  return true;
};